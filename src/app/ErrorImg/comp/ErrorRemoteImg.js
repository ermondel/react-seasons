import React from 'react';
import imgRemoteError from '../assets/remote-error.png';

const ErrorRemoteImg = () => (
  <img src={imgRemoteError} alt='The remote server is not responding' />
);

export default ErrorRemoteImg;
