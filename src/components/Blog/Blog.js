import React from 'react';
import BlogSidebar from './BlogSidebar';
import PostList from './PostList';

const Blog = () => (
  <main className='main'>
    <BlogSidebar />
    <PostList />
  </main>
);

export default Blog;
